let maxWidth = 680; //초기 이미지 로드시 가로 최대 크기
let canvas;
let object, object2, object3, bel, el, we;
let imageChange = false;

let scaleX=1;
let scaleY=1;

let globalColor='#000000';

let drawSelected=false;
let drawFlag=false;

let groupObject;