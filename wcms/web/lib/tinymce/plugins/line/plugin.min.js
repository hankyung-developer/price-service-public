tinymce.PluginManager.add('line', function(editor, url) {
    editor.ui.registry.addButton( 'line', {
    icon: 'more-drawer', 
    tooltip: '라인',
    onAction:function(){
        editor.insertContent("<hr class='line'></hr>");
      }
    });
  });